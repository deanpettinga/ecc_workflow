name: ecc_caller
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - bedtools
  - bwa
  - cutadapt
  - numpy
  - pandas
  - parallel
  - python=3.8.5
  - samtools
  - scipy
variables:
  ECC_CALLER_PYTHON_SCRIPTS: ./envs/ecc_caller/python_scripts
